# Getting page version through variables

Use `{{PAGEVERSION}}` variable to get version of the current page

Use `{{#PAGEVERSION:PageName}}` parser function to get version of a specific page.

If the page does not exist, or has no version, it will return empty string.